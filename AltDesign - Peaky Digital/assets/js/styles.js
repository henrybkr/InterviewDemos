/* Main JS file for styling */

document.addEventListener("DOMContentLoaded", function() {
    var servicesTabs = document.getElementsByClassName("service-tab");
    /*
    for (let element of servicesTabs) {
        console.log(element.id);
        element.addEventListener('mouseenter', event => {
            element.classList.add("animate__pulse");
        })
    }
    */

    /*servicesTabs.forEach(element => 
        console.log(element)
    );*/

    /*document.querySelector('.service-tab').addEventListener('mouseenter', event => {
        alert("ok");
    })*/
});